# TODO

## 🚀 Ready


## 🛠️ In Progress


## ⏳ Pending
- [ ] Comando `pipkg prune` para limpiar dependencias transitivas huérfanas tras un uninstall.
- [ ] Comando `pipkg check` para detectar dependencias declaradas pero no usadas en el código
- [ ] Comando `pipkg init` - Debe crear la carpeta y hacer `cd` automáticamente, para no tener que hacer `mkdir proyecto && cd proyecto && pipkg init`.
- [ ] Configuracion y uso de `pyproject.toml` para metadata del proyecto y dependencias, en lugar de `pipkg.toml`.

