# TODO

## 🚀 Ready


## 🛠️ In Progress


## ⏳ Pending
- [ ] Comando `ppm prune` para limpiar dependencias transitivas huérfanas tras un uninstall.
- [ ] Comando `ppm check` para detectar dependencias declaradas pero no usadas en el código
- [ ] Comando `ppm init` - Debe crear la carpeta y hacer `cd` automáticamente, para no tener que hacer `mkdir proyecto && cd proyecto && ppm init`.
- [ ] Configuracion y uso de `pyproject.toml` para metadata del proyecto y dependencias, en lugar de `ppm.toml`.

